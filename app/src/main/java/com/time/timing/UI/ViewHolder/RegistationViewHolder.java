package com.time.timing.UI.ViewHolder;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;

import com.time.timing.databinding.RegistationweekiteamBinding;

public class RegistationViewHolder extends RecyclerView.ViewHolder {

    public RegistationweekiteamBinding binding;

    public RegistationViewHolder(@NonNull RegistationweekiteamBinding binding) {
        super(binding.getRoot());
        this.binding = binding;
    }
}
